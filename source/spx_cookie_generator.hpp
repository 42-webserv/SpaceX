#pragma once

#include <iostream>
#include <map>
#include <sstream>

class cookie_generator {

	typedef std::pair<std::string, std::string> key_val;
	std::stringstream							contents;
	std::map<key_val>							map;

	cookie_generator() {};
	cookie_generator()
};
